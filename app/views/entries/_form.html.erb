<%= form_for(@entry) do |f| %>
  <% if @entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% @entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>




  <div class="clearfix">
    <%= f.label :name %>
    <div class="input">
      <%= f.text_field :name, class: "xxlarge"%>
    </div>

  </div>
  <div class="clearfix">
    <%= f.label :definition %>
    <div class="input">
      <%= f.text_area :definition, class: "xxlarge", rows: "5" %>
    </div>
  </div>

  <div class="actions">
    <%= f.submit class: "btn primary" %>
    <%= link_to "Cancel", entries_path, class: "btn" %>
  </div>


<% end %>
